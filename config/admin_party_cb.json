{
    "log": ["REST+"],
	"databases": {
		"db": {
			"server": "http://cbadminbucket:password@s61405cnt72.sc.couchbase.com:8091",
			"sync": `
function(doc){
	channel(doc.channels);
}`,
			"users": {
				"GUEST": {"disabled": false, "admin_channels": ["*"] }
			}
		}
	}
}
